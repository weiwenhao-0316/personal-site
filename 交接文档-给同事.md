# 个人站点项目交接文档

> 本文档已于 2026-08-25 更新至当前部署状态（阿里云 + 宝塔），与源代码核对一致。
> 部署细节与常见问题见 `docs/server-deploy-guide.md`；给 AI 助手的版本见 `AI开发提示词.md`。

## 一、项目概览

- **域名**：haoriver.site（腾讯云，已备案），DNS A 记录 @ 和 www 指向阿里云服务器 `120.77.2.164`
- **GitHub**：weiwenhao-0316/personal-site（服务器用 HTTPS 拉取）
- **部署**：阿里云 2核2G Ubuntu + 宝塔面板
  - 前端：`frontend/dist` 由 Nginx 提供静态服务（含 SSL、SPA 路由兜底）
  - 后端：FastAPI 由宝塔 Python 项目管理器运行（项目名 `haoriver-backend`，监听 127.0.0.1:8000）
  - 数据库：MySQL，库 `haoriver`，已有表 `collections`
- **技术栈**：Vue 3 + Vite + Vue Router（前端）/ FastAPI + PyMySQL + DeepSeek API（后端）
- **数据链路**：浏览器 → Nginx(443) → 静态页走 dist；`/api` 请求反向代理 → FastAPI(8000) → MySQL

> 注意：线上**没有** api 子域名，API 走同源 `/api` 反向代理（`.env.production` 里是 `https://haoriver.site`）。

---

## 二、目录结构

```
D:\claude test\personal-site\
├── frontend/                        # Vue 3 前端（主要开发区域）
│   ├── src/
│   │   ├── main.js                  # Vue 入口
│   │   ├── App.vue                  # 根组件（导航 + 路由出口）
│   │   ├── style.css                # 全局样式 + :root CSS 变量（设计系统）
│   │   ├── components/
│   │   │   └── NavBar.vue           # 顶部导航栏
│   │   ├── composables/
│   │   │   ├── useCollectionStore.js  # 收藏：调后端 /api/collections
│   │   │   ├── useNotesStore.js       # 笔记：浏览器本地
│   │   │   ├── useLibraryStore.js     # 文件库：浏览器本地
│   │   │   ├── useVault.js            # 仓库：IndexedDB
│   │   │   └── useWallpaperStore.js   # 壁纸：浏览器本地
│   │   ├── data/siteContent.js      # 站点内容数据
│   │   ├── router/index.js          # 路由配置（7 页 + 2 重定向）
│   │   └── views/                   # Home / Collection / Notes / Library / Chat / Projects / Vault
│   ├── public/exam/                 # 备考资料 docx（体积大，勿删）
│   ├── .env                         # 开发：VITE_API_URL=http://localhost:8000
│   ├── .env.production              # 生产：VITE_API_URL=https://haoriver.site
│   └── dist/                        # 构建产物（线上站点根目录）
├── backend/
│   ├── main.py                      # FastAPI：/api/health、/api/chat、注册 collections 路由
│   ├── collections_api.py           # 收藏 CRUD 四接口（PyMySQL 连 MySQL）
│   ├── requirements.txt             # fastapi, uvicorn, openai, python-dotenv, pymysql
│   └── .env.example                 # 环境变量模板（真实 .env 只存在于服务器）
├── docs/server-deploy-guide.md      # 宝塔部署详细指南
└── .qoder/skills/baota-deploy/      # 部署经验 Skill
```

---

## 三、页面当前状态

| 页面 | 路由 | 数据存储 | 说明 |
|------|------|---------|------|
| Home | `/` | - | 首页 |
| Collection | `/collection` | ✅ 服务器 MySQL | 已完成 localStorage → MySQL 迁移 |
| Notes | `/notes` | 浏览器本地 | 待迁移 |
| Library | `/library` | 浏览器本地 | 待迁移 |
| Chat | `/chat` | DeepSeek SSE | 线上可用 |
| Projects | `/projects` | 硬编码 | 4 个项目卡片 |
| Vault | `/vault` | IndexedDB | 待迁移 |
| /blog | 重定向 → `/notes` | - | 旧路由 |
| /tools | 重定向 → `/library` | - | 旧路由 |

## 四、后端接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/health` | GET | 健康检查，返回 `{"status":"ok"}` |
| `/api/collections` | GET / POST | 收藏查询 / 新增 |
| `/api/collections/{id}` | PUT / DELETE | 收藏更新 / 删除 |
| `/api/chat` | POST | DeepSeek 聊天（SSE 流式） |
| `/api/docs` | GET | Swagger 测试页（特意放在 /api 前缀下） |

后端环境变量（服务器 `backend/.env`）：`DB_HOST / DB_PORT / DB_USER / DB_PASSWORD / DB_NAME`、`DEEPSEEK_API_KEY / DEEPSEEK_BASE_URL / DEEPSEEK_MODEL`、`CORS_ORIGINS`、`PORT=8000`

---

## 五、服务器关键路径

| 项目 | 路径 |
|------|------|
| 项目目录 | `/www/wwwroot/haoriver/personal-site` |
| 后端环境变量 | `/www/wwwroot/haoriver/personal-site/backend/.env`（不走 Git，服务器直接改） |
| Nginx 站点配置 | `/www/server/panel/vhost/nginx/haoriver.site.conf` |
| 站点根目录 | `/www/wwwroot/haoriver/personal-site/frontend/dist` |

---

## 六、本地开发流程

```bash
# 1. 前端
cd "D:\claude test\personal-site\frontend"
npm run dev          # http://localhost:5173，热更新

# 2. 后端（收藏/Chat 页面需要）
cd "D:\claude test\personal-site\backend"
pip install -r requirements.txt
python main.py       # http://localhost:8000
```

改页面 → `frontend/src/views/xxx.vue`；改导航 → `components/NavBar.vue`；改全局样式 → `style.css`；新增页面 → 建 `views/*.vue` + 在 `router/index.js` 注册。

---

## 七、部署更新流程（每次上线遵循）

```bash
# 1. 本地提交推送（commit 用 conventional commits 格式）
git add .
git commit -m "feat: 描述改动"
git push

# 2. 服务器拉取并验证（FinalShell）
cd /www/wwwroot/haoriver/personal-site
git pull
git log --oneline -2          # 确认拉到了刚推的提交

# 3. 按改动类型发布
#    前端改动 → cd frontend && npm run build
#    后端改动 → 宝塔 Python 项目管理器重启（重启前先检查 8000 端口有无旧进程占用）

# 4. git pull 网络失败时：用镜像前缀（如 gh-proxy.com）或本地打包 dist 上传覆盖

# 5. 验证
curl http://127.0.0.1:8000/api/health    # 应返回 {"status":"ok"}
# 浏览器打开 https://haoriver.site 抽查功能
```

---

## 八、关键注意事项

1. **.env 含密钥** — 绝不写进代码、绝不提交 Git，只在服务器上维护
2. **同源 /api 反代** — 没有 api.haoriver.site，改 Nginx 时别弄丢 `location /api` 反代和 `try_files` 配置
3. **服务器内存小（约 1.6G）** — 别让多余服务（PHP 等）常驻，资源耗尽会卡死面板；详见部署指南 10.3
4. **public/exam/ 大文件** — docx 体积大，不要随意删除
5. **不确定服务器代码版本时** — 先 `git pull` + `git log` 验证，再动手

---

## 九、待办事项

- [ ] Notes / Library / Vault 数据迁移到 MySQL（参照收藏功能的迁移方式）
- [ ] Projects 页面改为动态数据
- [ ] 移动端体验优化
