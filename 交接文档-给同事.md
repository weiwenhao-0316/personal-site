# haoriver.site 项目交接文档

> 最后核对时间：2026-08-30
> 交接对象：下一位 AI Agent 或协作者
> 当前代码基线：`19acea7`
> 重要说明：本文只记录已经从代码、Git 和项目文档核对到的事实。线上没有实际检查到的内容统一标记为“待确认”。

## 一、项目概览

- **域名**：`haoriver.site`，DNS 指向阿里云 ECS。
- **GitHub**：`weiwenhao-0316/personal-site`，服务器使用 HTTPS 拉取。
- **部署**：阿里云 ECS + 宝塔面板。
  - 前端：`frontend/dist` 由 Nginx 提供静态服务，包含 SSL 和 SPA 路由兜底。
  - 后端：FastAPI 由宝塔 Python 项目管理器运行，项目名 `haoriver-backend`，监听 `127.0.0.1:8000`。
  - 数据库：MySQL，数据库名 `haoriver`。
- **技术栈**：Vue 3 + Vite + Vue Router / FastAPI + PyMySQL + DeepSeek API。
- **数据链路**：浏览器 → Nginx(443) → 静态页或 `/api` 反向代理 → FastAPI(8000) → MySQL / DeepSeek。
- **线上接口形态**：没有独立的 API 子域名，所有后端接口必须使用 `/api` 前缀。

## 二、当前产品方向

网站是一个以个人学习和生产为主、兼顾对外展示的数字收纳空间，用于收纳外部收藏、学习笔记、资料和项目记录，逐步形成“输入、整理、输出”的内容闭环。

第一阶段的目标是先跑通个人版本的核心闭环：

1. 能保存和管理收藏；
2. 能保存和管理笔记；
3. 临时收纳的内容未来可以整理为收藏或笔记；
4. 核心内容刷新页面后仍然存在；
5. 文档清楚说明已完成、待确认和暂缓的内容。

产品定义和页面职责见 `docs/product-spec.md`。产品范围仍是第一版草案，不要把未来想法直接当成当前需求。

## 三、目录结构

```text
personal-site/
├── frontend/
│   ├── src/views/              # Home/Collection/Notes/Library/Chat/Projects/Vault 等页面
│   ├── src/components/        # 可复用组件，例如 NavBar
│   ├── src/composables/        # API 调用和 localStorage/IndexedDB 数据逻辑
│   ├── src/router/index.js     # 路由表
│   ├── src/data/siteContent.js # 静态展示内容
│   ├── .env                    # 本地配置，当前实际指向线上 API，不要提交
│   └── .env.production         # 生产构建配置，不要提交敏感内容
├── backend/
│   ├── main.py                 # FastAPI 入口、health、chat 和路由注册
│   ├── collections_api.py      # 收藏 CRUD 与封面抓取
│   ├── notes_api.py            # 笔记 CRUD
│   ├── requirements.txt        # Python 依赖
│   ├── .env                    # 真实密钥和数据库配置，不进 Git
│   └── .env.example            # 环境变量模板，内容仍需后续补全
├── docs/
│   ├── product-spec.md         # 产品定义、页面职责和第一阶段边界
│   ├── roadmap.md              # 路线图、任务卡和当前进度
│   ├── migration-notes.sql     # Notes 表和种子数据脚本
│   ├── server-deploy-guide.md  # 宝塔部署指南
│   └── ai-handoffs/            # AI 交接模板及 ZCode/Qoder 核对记录
├── skills/
│   └── project-development-workflow/SKILL.md
├── AGENTS.md                  # 每个 Agent 必须遵守的仓库规则
├── README.md                  # 给人阅读的项目介绍和运行方式
└── AI开发提示词.md / 网站架构设计文档.md
```

## 四、页面和存储状态

| 页面 | 路由 | 当前数据来源 | 当前状态 |
|---|---|---|---|
| Home | `/` | 静态配置 + 页面展示 | 可用，首页内容仍需逐步真实化 |
| Collection | `/collection` | FastAPI + MySQL | CRUD 代码完成，线上最终状态待确认 |
| Notes | `/notes` | FastAPI + MySQL | CRUD 代码完成，线上终验待确认 |
| Library | `/library` | `localStorage` | 可用，本阶段暂不迁移 |
| Chat | `/chat` | FastAPI + DeepSeek SSE | 基础聊天可用，鉴权、限流未完成 |
| Projects | `/projects` | 静态配置 | 当前 3 个项目卡片，暂不做动态化 |
| Vault | `/vault` | `IndexedDB` | 本地临时收纳，文件上云方案未开始 |
| Wallpaper | 首页能力 | `IndexedDB` | 本地壁纸功能 |
| `/blog` | 重定向到 `/notes` | - | 兼容旧路由 |
| `/tools` | 重定向到 `/library` | - | 兼容旧路由 |

## 五、后端接口

| 方法 | 接口 | 说明 | 定义位置 |
|---|---|---|---|
| GET | `/api/health` | 健康检查，返回状态和当前提交号 | `backend/main.py` |
| POST | `/api/chat` | DeepSeek SSE 流式聊天 | `backend/main.py` |
| GET/POST | `/api/collections` | 收藏列表 / 新增 | `backend/collections_api.py` |
| PUT/DELETE | `/api/collections/{id}` | 收藏更新 / 删除 | `backend/collections_api.py` |
| GET/POST | `/api/notes` | 笔记列表 / 新增 | `backend/notes_api.py` |
| PUT/DELETE | `/api/notes/{id}` | 笔记更新 / 删除 | `backend/notes_api.py` |
| GET | `/api/docs` | Swagger 接口文档 | FastAPI 配置 |

接口规则：

- 新接口必须以 `/api` 开头，否则线上 Nginx 不会转发。
- SQL 使用参数占位符，数据库连接使用后必须关闭。
- 后端新功能优先单独建立 API 文件，再在 `main.py` 注册路由。
- 当前所有业务接口没有鉴权，公网环境下存在任何人读写数据的风险；这是已知暂缓项，不要在没有任务授权时顺手实现。

## 六、关键实现事实

### Collection

- 新增或更新收藏时，若封面为空且链接是 HTTP(S)，后端尝试抓取 `og:image`。
- B 站页面被机房 IP 拦截时，有官方数据接口兜底。
- 封面地址统一 HTTPS 化，失败时返回空字符串，前端使用渐变兜底。
- 图片使用 `referrerpolicy="no-referrer"`，用于减少图床防盗链问题。

### Notes

- `useNotesStore.js` 已从浏览器 `localStorage` 切换到 `/api/notes`。
- `backend/notes_api.py` 提供列表、新增、更新、删除接口。
- `docs/migration-notes.sql` 提供 `notes` 表和种子数据脚本，设计为可重复执行。
- 本地代码完成不等于线上生效。必须由用户在服务器执行拉取、建表或确认表存在、重启后端、前端构建，并在浏览器验证。

### 环境变量

后端 `.env` 实际需要：`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`、`DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME`、可选的 `CORS_ORIGINS` 和 `PORT`。

注意：当前 `frontend/.env` 实际为 `VITE_API_URL=https://haoriver.site`，所以本地开发时收藏和笔记请求可能直接写入线上 API。若要本地联调，先把本地配置改成 `http://localhost:8000`，不要把这个本地改动提交。

## 七、已验证与未验证

### 已在本地核对

- 当前代码基线为 `19acea7`。
- 前端 `npm run build` 已通过。
- `python -m compileall -q backend` 已通过语法编译检查。
- 本地代码包含 Collection 和 Notes 的 MySQL CRUD。
- `/api/health` 代码包含当前 commit 字段。
- 项目流程 Skill、产品规格、README 和 AI 交接目录已建立。

### 仍未在本轮确认

- 线上后端是否已经运行 `19acea7`。
- 线上 `notes` 表是否已经存在并可读写。
- Notes 线上页面的新增、编辑、删除和刷新持久化。
- Collection 封面抓取修复和原样更新不报 404 是否已经在线生效。
- 本地 Python 依赖是否在干净虚拟环境中满足版本约束。当前环境曾出现 FastAPI 与 Starlette 版本不兼容，后续应在项目虚拟环境重新安装 `backend/requirements.txt`。

## 八、部署和验证边界

标准流程见 `docs/server-deploy-guide.md` 和 `AGENTS.md`：

1. 本地完成代码、构建和检查；
2. 提交并推送 GitHub；
3. 用户在服务器执行 `git pull`；
4. 前端改动执行 `cd frontend && npm run build`；
5. 后端改动由用户在宝塔重启 `haoriver-backend`；
6. 用 `curl https://haoriver.site/api/health` 检查线上提交号，再抽查核心页面。

AI 不自动执行宝塔、Nginx、DNS、服务器重启、线上数据库脚本或生产部署，除非用户对具体操作明确授权。

## 九、当前未完成事项

| 优先级 | 事项 | 当前处理原则 |
|---|---|---|
| P0 | 线上确认 Notes 迁移和 Collection 修复 | 先做部署后的人工验收，不先扩展新功能 |
| P1 | API 鉴权、Chat 限流和输入限制 | 先跑通个人闭环，再单独设计最小主人模式 |
| P1 | 数据库备份 | 在数据量继续增加前安排每日备份 |
| P1 | Library 上云 | 先确定元数据、文件本体和旧本地数据迁移方式 |
| P1 | Vault 文件存储 | 先确定临时收纳和最终归档的边界 |
| P2 | 测试、Lint、CI/CD、监控 | 按稳定接口和发布风险逐步补齐 |
| P2 | 首页内容、移动端、Projects 动态化 | 等核心数据闭环稳定后再做 |

## 十、下一步建议

下一张任务卡是“Notes 线上终验与第一阶段闭环验收”：

- 用户在服务器执行 `git pull`，确认当前 commit；
- 按需执行 `docs/migration-notes.sql`；
- 重启后端并重新构建前端；
- 检查 `/api/health`、`/api/notes` 和 Notes 页面；
- 再检查 Collection 的读取、原样保存、封面和删除；
- 将实际结果回填到 `docs/roadmap.md` 和本目录的最新交接文档。

在这张任务卡完成前，暂不启动 Library 迁移、完整鉴权、AI 自动整理或大型架构重构。

## 十一、给下一位 Agent 的启动指令

开始工作前按这个顺序读取：

1. `AGENTS.md`
2. `skills/project-development-workflow/SKILL.md`
3. `docs/product-spec.md`
4. `docs/roadmap.md`
5. 本交接文档和 `docs/ai-handoffs/` 中与当前 Agent 相关的记录

先报告当前 Git 状态和线上验证边界，再创建一张明确的任务卡。不要把历史对话里的计划当作已经完成的事实，也不要因为发现新想法就扩大当前任务范围。
